export default ({spacing: {unit}}) => ({
  container: {
    margin: `${unit * 4}px auto 0`,
    width: 400
  },
  title: {
    marginBottom: unit * 2
  },
  icon: {
    margin: `0 auto ${unit * 2}`
  }
});